name: Test

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main
#       - develop

on:
  workflow_dispatch:

jobs:
  call-pytest-workflow:
    # Docs: https://github.com/ASFHyP3/actions
    uses: ASFHyP3/actions/.github/workflows/reusable-pytest.yml@v0.21.1
    permissions:
      contents: read
    with:
      local_package_name: hyp3_glacier_melt
      python_versions: >-
        ["3.10", "3.11", "3.12", "3.13"]
